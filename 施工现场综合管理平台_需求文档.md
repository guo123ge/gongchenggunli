# 施工现场综合管理平台 — 产品需求文档（PRD）

> **版本**：v1.0  
> **创建日期**：2026-05-31  
> **产品定位**：面向施工单位的现场综合管理工具，覆盖施工日志、材料、机械、安全、档案、变更签证六大业务模块  
> **设计哲学**：借鉴 EvidenceVault 的本地优先 + 暗色主题 + AI 增强思路，升级为多角色协同 + 审核流的团队级工具  
> **目标用户**：项目经理、施工员、技术员、安全员、材料员、资料员

---

## 一、背景与目标

### 1.1 现状痛点

当前施工现场管理普遍存在以下问题：

| 痛点 | 描述 |
|------|------|
| **纸质化严重** | 施工日志、材料台账、安全检查表仍以纸质为主，事后补录、字迹潦草、查找困难 |
| **信息孤岛** | 材料员管材料、安全员管安全，各岗位数据互不相通，项目经理看不到全局 |
| **签证变更失控** | 设计变更单、工程签证单流转慢，签字审批周期长，结算时经常扯皮 |
| **档案散落** | 图纸、合同、检测报告分散在微信群、U盘、个人电脑，版本无从追溯 |
| **无审核机制** | 现场记录缺乏有效审核，数据质量参差不齐，出了问题难以追责 |

### 1.2 产品目标

搭建一个**施工现场数字化协同平台**，实现：

1. **六个模块全覆盖** — 日志、材料、机械、安全、档案、变更签证全线数字化
2. **多角色分工协作** — 六大岗位各司其职，数据归口清晰
3. **项目经理审核** — 关键记录需经理审核后方可归档，确保数据质量
4. **本地优先 + 离线可用** — 延续 EvidenceVault 的技术基因，施工现场网络不稳定也能正常使用
5. **AI 辅助提效** — OCR 识别单据、AI 分析安全风险、智能归档建议

---

## 二、用户角色与权限矩阵

### 2.1 角色定义

| 角色 | 代号 | 职责 | 可录入模块 | 可修改范围 | 审核权限 |
|------|------|------|-----------|-----------|---------|
| **项目经理** | PM | 全局管理、审核把关 | 所有模块 | 所有数据 | 审核通过/驳回/退回修改 |
| **施工员** | CON | 现场施工管理 | 施工日志 | 本人录入数据 | 无 |
| **技术员** | TECH | 技术方案、图纸管理 | 设计变更、签证变更 | 本人录入数据 | 无 |
| **安全员** | SAFE | 安全检查、隐患排查 | 安全隐患、安全事件 | 本人录入数据 | 无 |
| **材料员** | MAT | 材料收发管理 | 材料进出库 | 本人录入数据 | 无 |
| **资料员** | DOC | 资料归档管理 | 档案资料（所有类别） | 本人录入数据 | 无 |
| **机械管理员** | MACH | 机械设备管理 | 机械进出库 | 本人录入数据 | 无 |

> **设计原则**：每个角色只能修改自己录入的数据，不能修改他人数据。项目经理拥有审核权和全部数据的查看权，但不直接修改一线人员录入的原始记录（审核通过后记录锁定）。

### 2.2 权限矩阵

```
               施工日志  材料进出库  安全隐患  档案资料  机械进出库  变更签证  审核
项目经理        查看       查看       查看      查看      查看       查看     ✓
施工员         录入/改     —         —        —        —         —        —
技术员          —         —         —        —        —       录入/改   —
安全员          —         —       录入/改    —        —         —        —
材料员          —       录入/改     —        —        —         —        —
资料员          —         —         —      录入/改    —         —        —
机械管理员       —         —         —        —      录入/改     —        —
```

---

## 三、功能模块详细规格

### 模块 A：施工日志管理

#### A.1 使用者：施工员（录入）、项目经理（审核）

#### A.2 核心功能

```
┌─────────────────────────────────────────────────┐
│                 施工日志管理                      │
├─────────────────────────────────────────────────┤
│  📝 日志录入    │  每日施工日志，含文字+照片+语音   │
│  📸 现场拍照    │  自动添加水印（时间+GPS+桩号）    │
│  🎤 语音转文字  │  现场口述，AI 转结构化日志       │
│  📋 模板库     │  预设常用施工场景模板             │
│  🔍 日志检索    │  按日期/工序/部位/天气筛选        │
│  ✅ 审核流转    │  施工员提交 → 项目经理审核        │
│  📊 工时统计    │  自动汇总人工/机械/材料用量       │
│  📤 导出       │  按周/月导出施工日志汇总表         │
└─────────────────────────────────────────────────┘
```

#### A.3 数据模型

```typescript
interface DailyLog {
  id: string;                    // UUID
  projectId: string;             // 所属项目
  workDate: Date;                // 施工日期
  weather: string;               // 天气（晴/阴/雨/雪/大风）
  temperature: { low: number; high: number };  // 温度范围
  
  // 施工内容
  workContent: string;           // 当日施工内容概述
  workPosition: string;          // 施工部位（桩号/楼层/区段）
  workProcess: string;           // 施工工序
  
  // 资源投入
  laborCount: number;            // 出勤人数
  laborDetail: string;           // 工种明细（钢筋工×10, 木工×8...）
  machineryUsed: string[];       // 使用机械
  materialUsed: { name: string; quantity: number; unit: string }[];
  
  // 质量安全
  qualityCheck: string;          // 质量检查情况
  safetyCheck: string;           // 安全检查情况
  
  // 附件
  photos: Attachment[];          // 现场照片（水印+GPS）
  audioNote?: Attachment;        // 语音备注
  
  // 审核
  status: 'draft' | 'submitted' | 'approved' | 'rejected';
  submittedBy: string;           // 施工员 ID
  reviewedBy?: string;           // 项目经理 ID
  reviewedAt?: Date;
  reviewComment?: string;
  
  createdAt: Date;
  updatedAt: Date;
}
```

#### A.4 验收标准

| # | 场景 | 验收条件 |
|---|------|---------|
| AC-A1 | 施工员新建日志 | 选择日期→填写内容→上传照片（自动加水印）→提交，状态变为"待审核" |
| AC-A2 | 照片水印 | 上传的照片自动叠加水印：项目名称+拍摄时间+GPS坐标+施工部位 |
| AC-A3 | 语音转文字 | 点击录音按钮→口述施工内容→AI 识别并填入对应字段（内容/部位/工序） |
| AC-A4 | 模板快捷录入 | 选择"混凝土浇筑"模板→自动填充常见工序、检查项→施工员补充修改 |
| AC-A5 | 项目经理审核 | 查看日志详情→审核通过→记录锁定，施工员不可再修改 |
| AC-A6 | 驳回修改 | 审核不通过→填写驳回原因→施工员收到通知→修改后重新提交 |
| AC-A7 | 日志检索 | 按日期范围+施工部位+天气组合筛选，1秒内出结果 |
| AC-A8 | 工时统计 | 选择周/月→自动汇总出勤人数、机械台班、材料用量 |

---

### 模块 B：材料进出库管理

#### B.1 使用者：材料员（录入）、项目经理（审核）

#### B.2 核心功能

```
┌─────────────────────────────────────────────────┐
│               材料进出库管理                      │
├─────────────────────────────────────────────────┤
│  📥 材料进场登记  │  供应商/批次/数量/合格证       │
│  📤 材料出库登记  │  领用班组/用途/数量            │
│  📦 库存实时看板  │  按材料类别展示当前库存         │
│  📄 单据OCR识别  │  拍照入库单/出库单，AI提取信息   │
│  ⚠️ 低库存预警   │  低于安全库存自动提醒           │
│  📊 材料台账     │  按材料+时间段生成收发存报表     │
│  ✅ 审核流转     │  材料员提交 → 项目经理审核       │
└─────────────────────────────────────────────────┘
```

#### B.3 数据模型

```typescript
// 材料字典
interface Material {
  id: string;
  projectId: string;
  name: string;                  // 材料名称
  category: string;              // 分类（钢筋/混凝土/木材/防水/装饰/其他）
  spec: string;                  // 规格型号
  unit: string;                  // 单位（吨/m³/㎡/m/根/套）
  safetyStock: number;           // 安全库存量
  currentStock: number;          // 当前库存（实时计算）
}

// 入库单
interface StockIn {
  id: string;
  projectId: string;
  materialId: string;
  billNo: string;                // 单据编号
  supplier: string;              // 供应商
  quantity: number;
  unitPrice?: number;            // 单价
  totalAmount?: number;          // 总金额
  batchNo?: string;              // 批次号
  certificateUrl?: string;       // 合格证/检测报告
  photos: Attachment[];          // 入库单据照片
  ocrResult?: OCRResult;         // AI OCR 识别结果
  
  status: 'draft' | 'submitted' | 'approved' | 'rejected';
  submittedBy: string;           // 材料员 ID
  reviewedBy?: string;
  reviewedAt?: Date;
  createdAt: Date;
}

// 出库单
interface StockOut {
  id: string;
  projectId: string;
  materialId: string;
  billNo: string;
  receiver: string;              // 领用班组/人员
  purpose: string;               // 用途（浇筑/绑扎/支护...）
  quantity: number;
  
  status: 'draft' | 'submitted' | 'approved' | 'rejected';
  submittedBy: string;
  reviewedBy?: string;
  reviewedAt?: Date;
  createdAt: Date;
}
```

#### B.4 验收标准

| # | 场景 | 验收条件 |
|---|------|---------|
| AC-B1 | 材料进场登记 | 选择材料→填写数量/供应商→拍照入库单→AI OCR 自动提取数量/单价→确认提交 |
| AC-B2 | OCR 识别入库单 | 上传采购单照片→AI 识别供应商/材料名称/数量/金额→自动填入表单→用户核对修正 |
| AC-B3 | 材料出库 | 选择材料→填写领用人/用途/数量→系统校验库存充足→提交 |
| AC-B4 | 库存不足拦截 | 出库数量 > 当前库存时，系统拒绝并提示"库存不足，当前库存X" |
| AC-B5 | 低库存预警 | 材料库存 < 安全库存时，看板标红并在首页提醒 |
| AC-B6 | 实时库存看板 | 按分类展示各材料当前库存，支持搜索和排序 |
| AC-B7 | 材料台账报表 | 选择材料+时间范围→生成收发存汇总表（期初+入库-出库=期末） |

---

### 模块 C：安全隐患及事件管理

#### C.1 使用者：安全员（录入）、项目经理（审核）

#### C.2 核心功能

```
┌─────────────────────────────────────────────────┐
│             安全隐患及事件管理                     │
├─────────────────────────────────────────────────┤
│  🔍 隐患巡查记录  │  日常安全检查，拍照+描述+风险等级 │
│  ⚡ 事件上报      │  安全事故/未遂事件快速上报       │
│  🔧 整改跟踪      │  隐患→整改通知→整改回复→复查闭环 │
│  📊 风险热力图    │  按部位/类型展示隐患分布         │
│  🤖 AI 风险识别  │  上传现场照片，AI 识别安全隐患    │
│  📋 安全检查表    │  内置JGJ59等规范检查表模板       │
│  ⏰ 整改到期提醒  │  整改期限临近/逾期自动提醒       │
│  ✅ 审核流转      │  安全员提交 → 项目经理审核       │
└─────────────────────────────────────────────────┘
```

#### C.3 数据模型

```typescript
// 安全隐患
interface SafetyHazard {
  id: string;
  projectId: string;
  title: string;
  description: string;
  location: string;              // 隐患位置
  riskLevel: 'low' | 'medium' | 'high' | 'critical';  // 风险等级
  category: string;              // 类别（高处坠落/物体打击/坍塌/触电/机械伤害/其他）
  photos: Attachment[];
  aiAnalysis?: AIHazardResult;   // AI 风险识别结果
  
  // 整改信息
  rectificationRequired: string; // 整改要求
  rectificationDeadline: Date;   // 整改期限
  responsiblePerson: string;     // 整改责任人
  rectificationResult?: string;  // 整改回复
  rectificationPhotos?: Attachment[];
  rectifiedAt?: Date;
  
  status: 'open' | 'rectifying' | 'rectified' | 'verified' | 'closed';
  submittedBy: string;           // 安全员 ID
  reviewedBy?: string;
  reviewedAt?: Date;
  createdAt: Date;
}

// 安全事件
interface SafetyIncident {
  id: string;
  projectId: string;
  title: string;
  incidentDate: Date;
  incidentLocation: string;
  incidentType: string;          // 类型（伤亡/财产损失/未遂/环境）
  severity: 'minor' | 'moderate' | 'major' | 'critical';
  description: string;
  casualties?: { injured: number; fatal: number };
  cause: string;
  handling: string;              // 处理措施
  photos: Attachment[];
  reportUrl?: string;            // 正式事故报告
  
  status: 'reported' | 'investigating' | 'handled' | 'closed';
  submittedBy: string;
  reviewedBy?: string;
  createdAt: Date;
}
```

#### C.4 验收标准

| # | 场景 | 验收条件 |
|---|------|---------|
| AC-C1 | 隐患登记 | 安全员填写隐患描述→选择风险等级→拍照→提交，红/橙/黄三色标识风险等级 |
| AC-C2 | AI 风险识别 | 上传现场照片→AI 识别安全帽/安全带/围栏/临边防护→标记缺失项→自动生成风险提示 |
| AC-C3 | 整改闭环 | 隐患→下发整改通知（指定责任人+期限）→责任人整改回复（上传整改照片）→安全员复查确认→关闭 |
| AC-C4 | 整改到期提醒 | 距离整改期限 ≤ 3天→黄色预警；逾期→红色告警，首页提醒 |
| AC-C5 | 事件上报 | 安全员填写事故信息→自动记录上报时间→提交审核→生成事件编号 |
| AC-C6 | 风险热力图 | 按施工区域展示隐患数量和风险等级分布，高发区域标红 |
| AC-C7 | 检查表模板 | 选择"脚手架检查表"→逐项勾选→自动生成检查结论→一键提交 |

---

### 模块 D：档案资料管理

#### D.1 使用者：资料员（录入）、项目经理（审核）

#### D.2 核心功能

```
┌─────────────────────────────────────────────────┐
│               档案资料管理                        │
├─────────────────────────────────────────────────┤
│  📂 分类归档      │  图纸/合同/检测报告/方案/交底... │
│  📤 文件上传      │  支持PDF/DWG/DOCX/XLSX/图片     │
│  🏷️ 智能标签     │  AI 自动提取关键信息并打标签     │
│  🔍 全文检索      │  搜索文件名+内容（文本型文件）    │
│  📋 版本管理      │  同一文件多版本，标注版本说明     │
│  🔗 关联引用      │  资料与施工日志/变更单关联       │
│  📊 归档完整性    │  按归档清单检查资料是否齐全       │
│  ✅ 审核流转      │  资料员提交 → 项目经理审核       │
└─────────────────────────────────────────────────┘
```

#### D.3 数据模型

```typescript
// 档案分类
type ArchiveCategory = 
  | 'drawing'         // 图纸（施工图/竣工图）
  | 'contract'        // 合同
  | 'test_report'     // 检测报告
  | 'construction_plan' // 施工方案
  | 'technical_disclosure' // 技术交底
  | 'acceptance_record' // 验收记录
  | 'quality_record'  // 质量记录
  | 'meeting_memo'    // 会议纪要
  | 'government_doc'  // 政府文件
  | 'other';

interface Archive {
  id: string;
  projectId: string;
  title: string;
  category: ArchiveCategory;
  subCategory?: string;          // 子分类
  
  // 文件
  files: ArchiveFile[];          // 支持多文件（含不同版本）
  
  // 元数据
  tags: string[];                // AI 自动标签 + 手动标签
  description?: string;
  relatedTo?: {                  // 关联引用
    type: 'dailyLog' | 'designChange' | 'visa' | 'safety';
    id: string;
  }[];
  
  // 签章/审批信息
  sealInfo?: {
    hasSeal: boolean;            // 是否盖章
    sealType?: string;           // 公章类型
  };
  
  status: 'draft' | 'submitted' | 'approved' | 'rejected';
  submittedBy: string;           // 资料员 ID
  reviewedBy?: string;
  reviewedAt?: Date;
  createdAt: Date;
  updatedAt: Date;
}

interface ArchiveFile {
  id: string;
  fileName: string;
  fileType: string;              // pdf/dwg/docx/xlsx/jpg/png...
  fileSize: number;
  version: number;               // 版本号
  versionNote?: string;          // 版本说明
  uploadedAt: Date;
  uploadedBy: string;
}
```

#### D.4 验收标准

| # | 场景 | 验收条件 |
|---|------|---------|
| AC-D1 | 档案上传 | 选择分类→上传文件→系统自动解析文件名提取信息→确认/补充→提交 |
| AC-D2 | AI 智能标签 | 上传施工方案 PDF→AI 提取关键词（如"深基坑""支护""降水"）→自动添加标签 |
| AC-D3 | 版本管理 | 上传新版图纸→系统自动标记版本号递增→旧版本保留可查 |
| AC-D4 | 全文检索 | 输入关键词"混凝土强度"→检索到文件名+内容包含该词的所有档案 |
| AC-D5 | 关联引用 | 在施工日志中可关联引用对应图纸/方案，点击直接跳转 |
| AC-D6 | 归档完整性检查 | 按归档清单（如竣工验收资料清单）逐项检查，缺失项标红提示 |
| AC-D7 | 在线预览 | PDF/图片类文件支持浏览器内直接预览，DWG文件提示下载 |

---

### 模块 E：机械进出库管理

#### E.1 使用者：机械管理员（录入）、项目经理（审核）

#### E.2 核心功能

```
┌─────────────────────────────────────────────────┐
│               机械进出库管理                      │
├─────────────────────────────────────────────────┤
│  🏗️ 机械台账      │  所有机械信息（型号/编号/产权）  │
│  📥 机械进场登记  │  进场日期/来源/状态/随机资料     │
│  📤 机械退场登记  │  退场日期/去向/使用台班统计     │
│  🔧 维修保养记录  │  保养周期/维修记录/下次保养日期  │
│  ⛽ 油耗/能耗记录 │  每日油耗/电耗登记              │
│  📊 台班统计     │  按机械+时间段自动统计台班       │
│  ⏰ 保养到期提醒  │  下次保养日期临近自动提醒       │
│  ✅ 审核流转      │  机械管理员提交 → 项目经理审核   │
└─────────────────────────────────────────────────┘
```

#### E.3 数据模型

```typescript
interface Machinery {
  id: string;
  projectId: string;
  name: string;                  // 机械名称
  category: string;              // 分类（起重/土方/混凝土/运输/其他）
  model: string;                 // 型号
  serialNo: string;              // 出厂编号
  owner: 'owned' | 'leased';    // 产权（自有/租赁）
  supplier?: string;             // 租赁方
  
  // 进出场
  entryDate: Date;
  exitDate?: Date;
  entryCondition: string;        // 进场状态
  entryPhotos: Attachment[];
  entryDocs?: Attachment[];      // 随机资料（合格证/检测报告）
  
  // 保养
  maintenanceCycle: number;      // 保养周期（天）
  lastMaintenanceDate?: Date;
  nextMaintenanceDate?: Date;
  maintenanceRecords: MaintenanceRecord[];
  
  // 台班/能耗
  shiftRecords: ShiftRecord[];   // 台班记录
  
  status: 'active' | 'maintenance' | 'exited';
  submittedBy: string;
  reviewedBy?: string;
  reviewedAt?: Date;
}

interface MaintenanceRecord {
  id: string;
  machineryId: string;
  date: Date;
  type: 'routine' | 'repair' | 'overhaul';
  description: string;
  cost?: number;
  photos?: Attachment[];
  nextMaintenanceDate?: Date;
  submittedBy: string;
}

interface ShiftRecord {
  id: string;
  machineryId: string;
  date: Date;
  shiftCount: number;            // 台班数
  fuelUsed?: number;             // 油耗（升）
  electricityUsed?: number;      // 电耗（度）
  workContent: string;           // 工作内容
  submittedBy: string;
}
```

#### E.4 验收标准

| # | 场景 | 验收条件 |
|---|------|---------|
| AC-E1 | 机械进场登记 | 选择/新增机械→填写进场日期/来源→拍照→上传随机资料→提交 |
| AC-E2 | 机械退场 | 标记退场日期→自动统计总台班数→生成使用汇总 |
| AC-E3 | 台班登记 | 选择机械→填写当日台班数/工作内容→提交 |
| AC-E4 | 保养到期提醒 | 距离下次保养 ≤ 7天→黄色预警；逾期→红色告警 |
| AC-E5 | 维修记录 | 选择机械→填写维修类型/描述/费用→更新下次保养日期 |
| AC-E6 | 机械看板 | 展示所有在场机械状态（正常运行/保养中/已退场），支持筛选 |

---

### 模块 F：设计变更及签证管理

#### F.1 使用者：技术员（录入）、项目经理（审核）

#### F.2 核心功能

```
┌─────────────────────────────────────────────────┐
│             设计变更及签证管理                     │
├─────────────────────────────────────────────────┤
│  📝 变更登记      │  设计变更/工程洽商登记          │
│  📋 签证管理      │  工程签证单/零星签证            │
│  📸 现场取照      │  签证事项现场拍照（自动水印）    │
│  💰 费用估算      │  变更/签证涉及费用估算          │
│  📊 变更/签证台账 │  按项目汇总所有变更签证         │
│  🔗 关联引用      │  签证与施工日志/档案关联         │
│  ⏰ 审批时效提醒  │  提交后N天未审批自动提醒         │
│  ✅ 审核流转      │  技术员提交 → 项目经理审核         │
└─────────────────────────────────────────────────┘
```

#### F.3 数据模型

```typescript
// 设计变更
interface DesignChange {
  id: string;
  projectId: string;
  changeNo: string;              // 变更编号
  title: string;
  changeType: 'design' | 'engineering';  // 设计变更/工程变更
  
  // 变更内容
  changeReason: string;          // 变更原因
  changeContent: string;         // 变更内容描述
  affectedScope: string;         // 影响范围
  drawings: ArchiveFile[];       // 变更图纸
  
  // 费用
  estimatedCost?: number;        // 估算费用
  actualCost?: number;           // 实际费用
  costBearing: string;           // 费用承担方
  
  // 相关方
  initiator: string;             // 发起方（甲方/设计/施工）
  relatedParties: string[];      // 涉及单位
  
  // 附件
  photos: Attachment[];
  attachments: Attachment[];     // 变更通知单等
  
  status: 'draft' | 'submitted' | 'approved' | 'rejected';
  submittedBy: string;
  reviewedBy?: string;
  reviewedAt?: Date;
  createdAt: Date;
}

// 工程签证
interface EngineeringVisa {
  id: string;
  projectId: string;
  visaNo: string;                // 签证编号
  title: string;
  visaType: 'standard' | 'material_machinery' | 'minor';  // 标准签证/工料机签证/零星签证
  
  // 签证内容
  visaReason: string;            // 签证原因
  visaContent: string;           // 签证内容
  constructionPosition: string;  // 施工部位
  
  // 工程量
  quantities: {
    item: string;                // 项目
    spec: string;                // 规格
    unit: string;                // 单位
    quantity: number;            // 数量
    unitPrice: number;           // 单价
    amount: number;              // 合价
  }[];
  totalAmount: number;           // 签证总金额
  
  // 现场证据
  photos: Attachment[];          // 现场照片（强制带水印）
  
  // 签章
  sealStatus: 'none' | 'contractor_sealed' | 'all_sealed';
  
  status: 'draft' | 'submitted' | 'approved' | 'rejected';
  submittedBy: string;
  reviewedBy?: string;
  reviewedAt?: Date;
  createdAt: Date;
}
```

#### F.4 验收标准

| # | 场景 | 验收条件 |
|---|------|---------|
| AC-F1 | 设计变更登记 | 填写变更编号/类型/原因→上传变更图纸→提交 |
| AC-F2 | 签证登记 | 填写签证类型→逐项添加工程量（项目/规格/单位/数量/单价系统自动计算合价）→拍照→提交 |
| AC-F3 | 签证照片水印 | 签证现场照片强制叠加水印（时间+GPS+签证编号），无照片不可提交 |
| AC-F4 | 签证费用自动计算 | 输入数量和单价→系统自动计算合价和总金额 |
| AC-F5 | 变更/签证台账 | 按项目筛选→展示所有变更和签证→按金额排序→支持导出 Excel |
| AC-F6 | 审批时效提醒 | 签证提交超过 7 天未审批→首页提醒项目经理 |
| AC-F7 | 关联引用 | 签证可关联对应日期的施工日志，证明签证事项确实发生 |

---

## 四、审核工作流

### 4.1 审核流程图

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  草稿     │────▶│  待审核   │────▶│  已通过   │────▶│  已归档   │
│ (draft)  │     │(submitted)│     │(approved)│     │(archived)│
└──────────┘     └────┬─────┘     └──────────┘     └──────────┘
                      │                                    
                      │ 驳回                              
                      ▼                                    
                 ┌──────────┐                              
                 │  已驳回   │────▶ 修改 ────▶ 待审核       
                 │(rejected)│                              
                 └──────────┘                              
```

### 4.2 审核规则

| 规则 | 说明 |
|------|------|
| R1 | 所有模块的关键记录（日志/入库/出库/隐患/事件/档案/机械/变更/签证）提交后进入"待审核" |
| R2 | 只有项目经理有审核权限 |
| R3 | 审核通过后，原始记录**锁定**，录入人不可再修改（如需修改，由项目经理"退回修改"） |
| R4 | 驳回时，项目经理必须填写驳回原因 |
| R5 | 驳回后的记录，录入人修改后重新提交，审核流程重新开始 |
| R6 | 草稿状态的记录只有录入人自己可见 |

### 4.3 移动端入口

系统为每个角色的**录入**和**审核**操作均预留移动端入口，支持在手机/平板等移动设备上完成核心操作：

| 角色 | 移动端录入 | 移动端审核 |
|------|-----------|-----------|
| **施工员** | 施工日志录入（拍照+语音转文字）、查看驳回记录 | — |
| **技术员** | 设计变更登记、签证录入（现场拍照取证） | — |
| **安全员** | 隐患巡查记录、事件上报（现场拍照） | — |
| **材料员** | 材料进场/出库登记（拍照 OCR 单据） | — |
| **资料员** | 档案文件上传、分类归档 | — |
| **机械管理员** | 机械进场/退场登记、台班记录 | — |
| **项目经理** | — | 审核所有模块的待审记录（通过/驳回/退回修改）、查看仪表盘 |

> **技术实现**：移动端采用 PWA（Progressive Web App）方案，与桌面端共享同一代码库（Next.js + Tailwind 响应式布局），无需单独开发 App。Service Worker 支持离线操作，恢复网络后自动同步。

### 4.4 草稿保存机制

每个角色在提交确认前，均可将录入内容**暂存为草稿**，避免现场工作中断导致数据丢失：

| 特性 | 说明 |
|------|------|
| **触发方式** | 表单页面提供"存为草稿"按钮（与"提交审核"并列） |
| **自动保存** | 表单内容变更后 3 秒自动保存草稿（防抖机制），无需手动操作 |
| **草稿恢复** | 重新进入录入页面时，自动检测未完成的草稿并提示恢复 |
| **草稿列表** | 首页"我的待提交"区域展示所有草稿，按最后修改时间排序 |
| **草稿可见性** | 草稿仅录入人自己可见，其他人（包括项目经理）不可见 |
| **草稿生命周期** | 提交审核后草稿自动转为"待审核"记录，原草稿清除；草稿可手动删除 |
| **离线草稿** | 网络中断时草稿自动存入 IndexedDB 本地缓存，恢复网络后可一键同步提交 |

---

## 五、全局功能

### 5.1 首页仪表盘（项目经理视角）

```
┌────────────────────────────────────────────────────────────┐
│  🏗️  XX项目 · 施工现场管理                    👤 PM 张三  │
├────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ 待审核    │ │ 安全隐患  │ │ 低库存    │ │ 保养到期     │  │
│  │   12 项   │ │   3 项   │ │   5 项    │ │   2 台       │  │
│  │ 🔴 2 逾期 │ │ 🔴 1 严重│ │ 🟡 预警   │ │ 🔴 1 过期    │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│                                                            │
│  📅 最近施工日志                     📊 本月材料进出       │
│  ├─ 05-30  混凝土浇筑（待审核）       ├─ 入库：32 笔        │
│  ├─ 05-29  钢筋绑扎（已通过）         ├─ 出库：28 笔        │
│  └─ 05-28  土方开挖（已通过）         └─ 库存金额：¥583万   │
│                                                            │
│  ⚡ 最新安全隐患                     📋 待审批签证           │
│  ├─ 临边防护缺失（整改中，剩余2天）    ├─ V2026-003 土方外运  │
│  └─ 脚手架连墙件不足（逾期3天！）      └─ V2026-004 基础加深  │
└────────────────────────────────────────────────────────────┘
```

### 5.2 首页仪表盘（一线人员视角）

简化版，聚焦自己的待办和提醒：
- 我的待修改（驳回记录）
- 我的待提交（草稿）
- 我负责的整改任务（安全员/责任人）
- 分配给我的审核提醒

### 5.3 AI 对话助手

```
┌─────────────────────────────────────────────┐
│  🤖 智慧工地助手                     [−] [×] │
├─────────────────────────────────────────────┤
│  我可以帮你：                                │
│  📝 "今天钢筋用了多少吨？"                   │
│  📊 "生成本周施工周报"                       │
│  🔍 "查找深基坑相关的支护方案"               │
│  ⚠️ "最近一周有哪些安全隐患？"               │
│  📋 "未审批的签证有几份，金额多少？"          │
└─────────────────────────────────────────────┘
```

---

## 六、技术架构参考

### 6.1 架构选型

与 EvidenceVault 不同，本系统是多用户协同场景，需要后端支持：

```
┌─────────────────────────────────────────────────┐
│                 客户端 (PWA)                     │
│  Next.js + React 19 + Tailwind 4 + Zustand      │
│  Dexie.js (IndexedDB 离线缓存)                   │
│  Service Worker (离线可用)                       │
└────────────────────┬────────────────────────────┘
                     │ HTTP/WebSocket
┌────────────────────┴────────────────────────────┐
│                 后端服务                          │
│  Node.js / Python (FastAPI)                     │
│  PostgreSQL (业务数据) + Redis (缓存/队列)       │
│  MinIO/S3 (文件存储)                             │
│  WebSocket (实时通知/审核推送)                   │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────┴────────────────────────────┐
│                 AI 服务层                        │
│  OCR 识别 (单据/照片)                            │
│  安全风险识别 (图像分析)                         │
│  智能标签/分类 (NLP)                             │
│  对话助手 (LLM + RAG)                            │
└─────────────────────────────────────────────────┘
```

### 6.2 与 EvidenceVault 的技术差异

| 维度 | EvidenceVault | 本系统 |
|------|-------------|--------|
| 架构 | 纯前端 Severless | 前端 + 后端 + DB |
| 多用户 | 单用户（个人工具） | 多角色协同 |
| 存储 | IndexedDB（纯本地） | PostgreSQL + 本地 IndexedDB 缓存 |
| 认证 | 无（激活码） | 用户账号 + 角色权限 |
| 离线 | PWA 离线 | PWA 离线（本地缓存→在线同步） |
| 同步 | 无 | 数据同步 + 冲突解决 |
| AI | 可选（用户自带 Key） | 系统集成 AI |

### 6.3 数据同步策略

```
在线模式:
  UI → Zustand → API → PostgreSQL
                  ↓
            IndexedDB (同步缓存)

离线模式:
  UI → Zustand → IndexedDB (本地写入)
                  ↓
         离线队列 (pending sync)
                  ↓ (恢复网络后)
              API → PostgreSQL
```

### 6.4 可复用的 EvidenceVault 设计

| 设计元素 | 复用方式 |
|----------|---------|
| 暗色主题 + 金色主色 | 保留配色方案，适配本系统 |
| 命令面板 ⌘K | 保留，融合六大模块的快速操作 |
| Dexie.js liveQuery | 本地缓存层使用 Dexie，在线数据同步 |
| 图片水印引擎 (Canvas) | 扩展水印内容（角色+时间+GPS+模块类型） |
| 状态驱动的 UI 配色 | 扩展到各模块（审核状态、风险等级、库存状态） |
| AI 对话助手 | 升级为施工场景专用知识问答 |

---

## 七、非功能需求

### 7.1 性能要求

| 指标 | 目标 |
|------|------|
| 页面首次加载 | < 3s（有缓存 < 1s） |
| 列表数据加载（1000条） | < 500ms |
| 照片上传+水印处理 | < 3s/张（10MB以下） |
| 全文检索响应 | < 1s |
| 离线可用 | Service Worker 缓存核心页面 |
| 并发用户 | 支持同一项目 50+ 用户同时在线 |

### 7.2 安全要求

| 层级 | 措施 |
|------|------|
| 传输 | HTTPS + JWT Token |
| 认证 | 账号密码 + 可选二次验证 |
| 授权 | 基于角色的访问控制（RBAC） |
| 数据 | 数据库字段级权限、操作日志审计 |
| 文件 | 上传类型白名单、病毒扫描、访问权限校验 |
| 备份 | 每日自动备份，支持手动导出项目数据 |

### 7.3 兼容性

- 浏览器：Chrome 90+、Edge 90+、Safari 15+、Firefox 90+
- 移动端：iOS Safari 15+、Android Chrome 90+
- 屏幕：响应式设计，最小支持 375px（iPhone SE）

---

## 八、迭代规划

### Phase 1 — MVP（核心闭环）

**工期建议：6-8 周**

```
┌──────────────────────────────────────────┐
│  用户系统 + 审核流 + 施工日志 + 材料管理   │
│                                          │
│  ✓ 用户注册/登录/角色分配                 │
│  ✓ 施工日志：录入 → 审核 → 归档           │
│  ✓ 材料进出库：入库/出库 → 审核 → 库存    │
│  ✓ 审核工作流引擎                         │
│  ✓ 首页仪表盘（待审核+待办）              │
│  ✓ PWA 离线缓存                           │
└──────────────────────────────────────────┘
```

### Phase 2 — 扩展模块

**工期建议：4-6 周**

```
┌──────────────────────────────────────────┐
│  安全管理 + 机械管理 + 档案管理            │
│                                          │
│  ✓ 安全隐患巡查 → 整改闭环                │
│  ✓ 安全事件上报                           │
│  ✓ 机械台账 + 进出场 + 保养               │
│  ✓ 档案资料上传/分类/检索                 │
│  ✓ AI OCR 单据识别                        │
└──────────────────────────────────────────┘
```

### Phase 3 — AI 增强 + 报表

**工期建议：4-6 周**

```
┌──────────────────────────────────────────┐
│  变更签证 + AI 能力 + 报表导出             │
│                                          │
│  ✓ 设计变更管理                           │
│  ✓ 签证管理 + 费用自动计算                │
│  ✓ AI 安全风险识别（照片分析）            │
│  ✓ AI 对话助手（施工知识问答）            │
│  ✓ 施工周报/月报自动生成                  │
│  ✓ 材料/机械/签证统计报表                 │
│  ✓ 数据导出（Excel/PDF）                  │
└──────────────────────────────────────────┘
```

---

## 九、项目数据结构

### 9.1 项目创建

```typescript
interface Project {
  id: string;
  name: string;                  // 项目名称
  code: string;                  // 项目编号
  location: string;              // 项目地点
  startDate: Date;               // 开工日期
  plannedEndDate: Date;          // 计划竣工日期
  
  // 相关方
  owner: string;                 // 建设单位
  contractor: string;            // 施工单位
  supervisor?: string;           // 监理单位
  
  // 项目成员
  members: {
    userId: string;
    role: 'PM' | 'CON' | 'TECH' | 'SAFE' | 'MAT' | 'DOC' | 'MACH';
    joinedAt: Date;
  }[];
  
  status: 'preparation' | 'in_progress' | 'suspended' | 'completed';
  createdAt: Date;
}
```

### 9.2 用户模型

```typescript
interface User {
  id: string;
  username: string;              // 登录账号
  displayName: string;           // 显示姓名
  phone: string;                 // 手机号
  avatar?: string;               // 头像
  
  // 全局角色（可参与多个项目，每个项目角色可不同）
  globalRole: 'admin' | 'user';
  
  createdAt: Date;
}
```

---

## 十、风险与依赖

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 施工现场网络不稳定 | 数据同步失败 | PWA 离线缓存 + 在线队列重试 |
| 用户抵触数字化 | 推行困难 | 简化操作流程，语音/拍照输入降低使用门槛 |
| AI 识别准确率不足 | OCR/风险识别误判 | 所有 AI 结果需人工确认，不自动生效 |
| 多项目数据隔离 | 数据泄露 | 严格的项目级数据隔离，用户加入项目才可见 |
| 文件存储成本 | 照片/文档持续增长 | 按项目归档，历史项目数据支持冷存储 |

---

## 附录 A：术语对照

| 术语 | 说明 |
|------|------|
| 证据链 | EvidenceVault 核心概念，指施工过程中形成的可用于结算的证据材料 |
| 签证 | 工程签证单，施工单位就合同外工程量向建设单位申请确认的文件 |
| 设计变更 | 由设计单位出具的设计变更通知单 |
| 洽商记录 | 多方就工程技术问题达成的书面协议 |
| 台班 | 机械工作 8 小时为 1 个台班 |
| 水印 | 照片上叠加的文字信息（时间/位置/编号），用于保证真实性 |
| 整改闭环 | 发现隐患→整改通知→整改回复→复查确认的完整流程 |

---

*本文档基于 EvidenceVault 项目分析报告的设计思路重构，面向施工现场多角色协同管理场景。技术架构吸收了纯前端的本地优先理念，升级为支持多用户的在线+离线混合架构。*
