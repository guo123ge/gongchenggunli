# EvidenceVault 项目构造与构建思路分析报告

> 分析日期：2026-05-31  
> 项目地址：https://evidence.cybertumu.com  
> 产品名称：证据链管理 - EvidenceVault  
> 定位：工程建设结算前证据链管理工具，专为施工单位预算造价人员设计

---

## 一、项目概况

EvidenceVault 是一个**纯前端、本地优先**的工程证据链管理系统。核心设计理念是"无需注册、无需服务器、数据完全存储在用户设备上"，通过 AI 多模态识别技术，为施工单位打造从拍照取证到资料归档、报告导出的全流程数字化闭环。

### 关键主张

| 维度 | 实现方式 |
|------|----------|
| 数据存储 | 浏览器 IndexedDB（Dexie.js），完全本地 |
| 运行方式 | PWA，离线可用 |
| 用户门槛 | 无需注册，打开浏览器即用 |
| 商业模式 | 1 天试用 + 激活码授权 |
| 安全性 | 端到端加密声明，本地优先存储 |
| 开放性 | 开源透明 |

---

## 二、技术架构

### 2.1 技术栈总览

```
┌─────────────────────────────────────────────────────────┐
│                    前端技术栈                             │
├─────────────────────────────────────────────────────────┤
│  框架:      Next.js (App Router + Turbopack)             │
│  语言:      TypeScript / React                          │
│  样式:      Tailwind CSS 4 (自定义暗色主题)               │
│  图标:      Lucide React (SVG 图标库)                    │
│  状态管理:  Zustand (轻量级 React 状态管理)               │
│  数据库:    Dexie.js v4.3.0 (IndexedDB 封装)             │
│  路由:      Next.js App Router                          │
│  PWA:       manifest.json + Service Worker              │
│  部署:      Vercel / 静态导出（推测）                     │
└─────────────────────────────────────────────────────────┘
```

### 2.2 核心依赖分析

从 JS chunk 反编译分析，项目使用以下关键库：

| 库 | 用途 | 版本 |
|----|------|------|
| `next` | 全栈框架 | 最新 (Turbopack) |
| `react` / `react-dom` | UI 框架 | 19.x |
| `dexie` | IndexedDB 封装 | 4.3.0 |
| `zustand` | 状态管理 | 最新 |
| `tailwindcss` | 原子化 CSS | 4.x |
| `lucide-react` | 图标组件 | 最新 |
| `uuid` | 唯一 ID 生成 | 最新 |
| `clsx` + `tailwind-merge` | 类名合并 | 最新 |

### 2.3 架构图

```
┌──────────────────────────────────────────────────────────────┐
│                        浏览器端 (Browser)                     │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────┐  │
│  │  Landing │  │Dashboard │  │ Projects │  │  Settings   │  │
│  │  Page    │  │  Page    │  │  Page    │  │  Page       │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬──────┘  │
│       │              │              │               │        │
│  ┌────┴──────────────┴──────────────┴───────────────┴────┐   │
│  │                   App Shell (布局层)                   │   │
│  │  ┌─────────┐  ┌──────────────┐  ┌─────────────────┐  │   │
│  │  │ Sidebar │  │ Command      │  │ AI Chat         │  │   │
│  │  │ Nav     │  │ Palette ⌘K   │  │ Assistant ⌘J    │  │   │
│  │  └─────────┘  └──────────────┘  └─────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
│                              │                               │
│  ┌───────────────────────────┴───────────────────────────┐   │
│  │                   状态管理层                            │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐│   │
│  │  │ Zustand      │  │ Dexie        │  │ localStorage ││   │
│  │  │ (UI 状态)    │  │ liveQuery    │  │ (配置/许可)  ││   │
│  │  └──────────────┘  └──────┬───────┘  └──────────────┘│   │
│  └───────────────────────────┼───────────────────────────┘   │
│                               │                              │
│  ┌───────────────────────────┴───────────────────────────┐   │
│  │                   数据持久层                            │   │
│  │  ┌──────────────────────────────────────────────────┐ │   │
│  │  │        IndexedDB (EvidenceVaultDB)               │ │   │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────────┐    │ │   │
│  │  │  │ projects │ │ evidence │ │  dailyLogs   │    │ │   │
│  │  │  └──────────┘ └──────────┘ └──────────────┘    │ │   │
│  │  └──────────────────────────────────────────────────┘ │   │
│  └───────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌───────────────────────────────────────────────────────┐   │
│  │                   许可 & 安全层                        │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │   │
│  │  │ Trial Mode   │  │ Activation   │  │ Watermark    │ │   │
│  │  │ (1 天试用)   │  │ Code System  │  │ (水印引擎)  │ │   │
│  │  └──────────────┘  └──────────────┘  └─────────────┘ │   │
│  └───────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

---

## 三、数据库设计

### 3.1 Dexie 数据库版本演进

```javascript
// 数据库名: EvidenceVaultDB

v1: 
  projects: "id, name, code, status, createdAt, updatedAt"
  evidence: "id, projectId, type, status, title, createdAt, updatedAt, 
             [projectId+type], [projectId+status], [projectId+createdAt]"

v2: 结构不变（Schema 兼容升级）

v3: 
  + dailyLogs: "id, projectId, workDate, createdAt, updatedAt, 
                [projectId+workDate]"
```

### 3.2 数据模型

#### Project（项目）
```
{
  id: string (UUID),
  name: string,          // 项目名称
  code: string,          // 项目编号
  status: "active" | "completed" | "archived",
  location?: string,     // 项目地点
  owner?: string,        // 建设单位
  contractor?: string,   // 施工单位
  createdAt: Date,
  updatedAt: Date
}
```

#### Evidence（证据）
```
{
  id: string (UUID),
  projectId: string,     // 关联项目
  type: EvidenceType,    // 证据类型（10 种）
  status: EvidenceStatus,// 状态（6 种）
  title: string,
  serialNumber?: string, // 编号
  amount?: number,       // 金额
  description?: string,  // 描述
  tags: string[],        // 标签
  deadline?: Date,       // 截止日期
  attachments?: [],      // 附件
  aiAnalysis?: {},       // AI 分析结果
  sealInfo?: {},         // 签章信息
  createdAt: Date,
  updatedAt: Date
}
```

#### DailyLog（施工日志）- v3 新增
```
{
  id: string (UUID),
  projectId: string,
  workDate: Date,
  title?: string,
  completedWork?: string,
  recorder?: string,
  createdAt: Date,
  updatedAt: Date
}
```

### 3.3 复合索引策略

索引设计体现了核心查询场景：

| 索引 | 查询场景 |
|------|----------|
| `[projectId+type]` | 按项目 + 证据类型过滤 |
| `[projectId+status]` | 按项目 + 状态过滤（待办中心核心查询） |
| `[projectId+createdAt]` | 按项目 + 时间排序 |
| `[projectId+workDate]` | 按项目 + 施工日期查询日志 |

---

## 四、功能模块详解

### 4.1 六大 AI 核心能力

```
┌─────────────────────────────────────────────────────────────┐
│                    AI 驱动的六大核心能力                       │
├───────────────┬───────────────┬─────────────────────────────┤
│ 1. 施工照片   │ 2. 签章印鉴   │ 3. OCR 文字                 │
│    智能分析   │    自动检测   │    智能提取                 │
│   📷 Camera   │   📋 FileCheck│   🧠 Brain                  │
│   场景/材料/  │   公章类型/   │   编号/金额/日期/           │
│   工序/安全   │   签名位置/   │   单位等关键字段            │
│               │   完整性评分  │                             │
├───────────────┼───────────────┼─────────────────────────────┤
│ 4. AI 对话    │ 5. 资料归档   │ 6. 结算导出                 │
│    助手       │    完整性校验 │    报告                     │
│   🤖 Bot      │   🔒 Lock     │   📊 ChartColumn            │
│   ⌘J 全局唤起│   元数据/摘要/│   PDF/Excel 导出            │
│   上下文感知  │   操作轨迹    │   AI 分析结果               │
└───────────────┴───────────────┴─────────────────────────────┘
```

### 4.2 四步全流程闭环

```
步骤 01              步骤 02              步骤 03              步骤 04
┌──────────┐        ┌──────────┐        ┌──────────┐        ┌──────────┐
│ 拍照上传  │  ────▶ │AI 智能解析│  ────▶ │ 审核填充  │  ────▶ │ 归档导出  │
│          │        │          │        │          │        │          │
│ 水印+GPS │        │OCR+场景  │        │逐字段勾选│        │生成报告  │
│ 自动添加 │        │+签章+质量│        │一键采纳  │        │沉淀资料  │
└──────────┘        └──────────┘        └──────────┘        └──────────┘
```

### 4.3 全局快捷键系统

| 快捷键 | 功能 | 实现 |
|--------|------|------|
| `⌘K` / `Ctrl+K` | 命令面板（全局搜索） | `useEffect` + `keydown` 监听 |
| `⌘J` / `Ctrl+J` | AI 对话助手 | 浮动 FAB 按钮 + 快捷键 |
| `ESC` | 关闭弹窗/面板 | 事件冒泡拦截 |

### 4.4 命令面板（Command Palette）

命令面板是系统的核心导航中枢，融合了四种搜索域：

```
┌─────────────────────────────────────────────┐
│  🔍 搜索命令、项目、证据、日志...     [ESC] │
├─────────────────────────────────────────────┤
│  命令 (Commands)                            │
│  ├─ 进入证据仓库              Dashboard     │
│  ├─ 项目管理                  Projects      │
│  ├─ 待处理中心                Pending       │
│  ├─ 结算报告 / 导出           Export        │
│  ├─ 系统设置                  Settings      │
│  ├─ 打开 AI 助手              ⌘J            │
│  ├─ 切换侧边栏                Toggle        │
│  ├─ 新建证据 · {项目名}       New Evidence  │
│  └─ 批量导入 Excel · {项目名} Excel Import  │
│                                             │
│  项目 (Projects)                            │
│  └─ 项目名 · 编号 · 地点                    │
│                                             │
│  证据 (Evidence)                            │
│  └─ 标题 · 类型 · #编号 · 项目 · 状态       │
│                                             │
│  施工日志 (Daily Logs)                      │
│  └─ 标题 · 日期 · 项目 · 记录人             │
│                                             │
│  ↑↓ 导航  ↵ 选中  ESC 关闭   共 N 项       │
└─────────────────────────────────────────────┘
```

### 4.5 提醒中心（Notification Center）

基于证据状态和风险评级的智能提醒：

```
┌─────────────────────────────────────────────┐
│  🔔 提醒中心                    4 PENDING   │
├─────────────────────────────────────────────┤
│  🔴 逾期 (Overdue)                          │
│  └─ 已逾期 N 天的证据项                     │
│                                             │
│  🟡 即将到期 (Urgent)                       │
│  └─ 剩余 ≤ 7 天的证据项                     │
│                                             │
│  🟣 争议项 (Disputed)                       │
│  └─ status === "disputed"                   │
│                                             │
│  🔵 低完整性 (Low Integrity)                │
│  └─ 完整性评分 < 60                         │
│                                             │
│  🟠 高风险 (High Risk)                      │
│  └─ 风险评分 ≥ 70                           │
└─────────────────────────────────────────────┘
```

### 4.6 AI 对话助手

```
┌─────────────────────────────────────────────┐
│  ✨ EvidenceVault AI                 [−] [×] │
├─────────────────────────────────────────────┤
│  💬 对话区域（支持 Markdown 渲染）           │
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │ 用户问：帮我检查证据完整性          │    │
│  └─────────────────────────────────────┘    │
│  ┌─────────────────────────────────────┐    │
│  │ AI 答：已分析项目「高新区道路改造」  │    │
│  │ ✓ 签章完整度 92/100                 │    │
│  │ ✓ 照片质量评分 85/100               │    │
│  │ ⚠ 2份签证单缺少监理签章             │    │
│  │ ✓ 关键资料已归档                    │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  快捷操作：[生成报告] [查看详情] [修复建议]  │
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │ 输入问题...                    [发送]│    │
│  └─────────────────────────────────────┘    │
│  AI_ASSISTANT · SHIFT+ENTER 换行            │
└─────────────────────────────────────────────┘
```

---

## 五、许可与激活系统

### 5.1 模式设计

```
┌──────────────────────────────────────────────────────┐
│                   许可状态机                          │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────┐    首次访问    ┌──────────┐            │
│  │  未授权   │ ────────────▶ │ 1天试用   │            │
│  │ (locked) │               │ (trial)  │            │
│  └──────────┘               └────┬─────┘            │
│       ▲                          │                   │
│       │         到期              │ 输入激活码        │
│       │                          ▼                   │
│  ┌────┴─────┐               ┌──────────┐            │
│  │ 已过期    │ ◀─────────── │  已激活   │            │
│  │ (expired)│   周期到期    │ (active) │            │
│  └──────────┘               └──────────┘            │
│                                                      │
│  存储: localStorage ("evidence_app_config")          │
│  绑定: 本机浏览器 (localStorage 域隔离)              │
│  激活周期: 预设天数（具体天数见源码 ACTIVATION_PERIOD_DAYS）│
└──────────────────────────────────────────────────────┘
```

### 5.2 激活码格式

- 格式：`XXXX-XXXX-XXXX-XXXX`（16 位大写字母数字，4 位一组用 `-` 分隔）
- 正则校验：`TRIAL_CODE_PATTERN`
- 白名单机制：`isTrialCodeAllowed()`

### 5.3 试用说明

- 首次进入自动开启 1 天试用
- 刷新网页不重置试用时间
- 到期后必须输入已发放的激活码
- 授权状态保存在本机浏览器
- 建议在设置页导出备份

---

## 六、页面路由结构

```
/                           Landing Page（首页）
├── /dashboard               证据仓库（主工作台）
├── /projects                项目管理
│   └── /projects/[id]       项目详情
│       ├── /evidence/new    新建证据
│       └── /evidence/[eid]  证据详情
├── /pending                 待处理中心
├── /export                  结算导出
├── /settings                系统设置
│   └── (包含 API Key 配置、角色切换、数据管理等)
└── /auth                    授权/激活（可能嵌入其他页面）
```

---

## 七、用户角色系统

```javascript
// 角色定义（从 JS 源码提取）
ROLE_LABELS: {
  contractor: "施工单位版",
  owner: "建设单位（甲方）版"
}

ROLE_DESCRIPTIONS: {
  contractor: "专为施工单位预算/造价人员设计的个人工具。",
  owner: "专为建设单位（甲方）项目管理人员设计的个人工具。"
}

ROLE_LANDING_TEXT: {
  contractor: "为施工单位打造从拍照取证到资料归档、报告导出的全流程数字化闭环",
  owner: "为建设单位（甲方）打造从签证审批到资料归档、报告导出的全流程数字化闭环"
}
```

角色切换会影响：
- 首页文案
- 功能入口
- AI 助手上下文

---

## 八、证据类型与状态体系

### 8.1 证据类型（10 种）

| 类型 Key | 中文名称 | 说明 |
|----------|----------|------|
| `visa` | 工程签证单 | 标准签证 |
| `material_visa` | 工料机签证 | 材料/机械相关 |
| `minor_visa` | 零星签证 | 小额/杂项 |
| `design_change` | 设计变更单 | 设计方变更 |
| `engineering_change` | 工程变更 | 施工方变更 |
| `contact_sheet` | 工程联系单/洽商记录 | 沟通记录 |
| `quantity_confirm` | 工程量确认单 | 量方确认 |
| `site_photo` | 现场照片/影像 | 影像证据 |
| `meeting_memo` | 会议纪要/备忘录 | 会议记录 |
| `other` | 其他 | 兜底类型 |

### 8.2 证据状态（6 种）

```
draft ──────▶ submitted ──────▶ countersigned ──────▶ used_in_settlement
  │                                                       │
  └───────────────────────────────────────────────────────┘
                           │
                     archived (归档)
                           │
                     disputed (争议)
```

| 状态 Key | 中文 | 视觉配色 |
|----------|------|----------|
| `draft` | 草稿 | 灰色 |
| `submitted` | 已提交 | 琥珀色 (amber) |
| `countersigned` | 已签回 | 翠绿色 (emerald) |
| `archived` | 已归档 | 蓝色 (blue) |
| `used_in_settlement` | 已用于结算 | 翠绿色 (emerald) |
| `disputed` | 有争议 | 玫红色 (rose) |

---

## 九、UI/UX 设计特征

### 9.1 设计系统

```
色彩体系:
  背景:  #0a0c10 (深黑蓝)
  卡片:  #0d0f15 (次级背景)
  主色:  #ffb800 (金色 - 品牌主色)
  辅助:  翡翠绿/紫罗兰/天蓝/玫红 (功能色)
  
效果:
  - 毛玻璃效果 (backdrop-blur)
  - 渐变边框 (gradient borders)
  - 微交互 (hover scale, active scale)
  - 动画入场 (fade-in + translate-y)
  - 扫描线效果 (scanline overlay)
  - 网格背景图案 (grid-pattern)
  - 径向渐变光晕 (radial-gradient blobs)
```

### 9.2 响应式设计

- 移动端适配（`max-w-6xl` 等容器）
- 侧边栏折叠
- FAB（浮动操作按钮）用于移动端 AI 助手
- 表格/网格自适应列数

### 9.3 暗色主题

全局 `dark` 模式，使用 Tailwind 的半透明颜色系统（`white/[0.04]` 等），实现丰富的层次感而不依赖大量色值定义。

---

## 十、核心工具函数

从 JS 源码提取的关键工具函数：

| 函数 | 功能 |
|------|------|
| `generateId()` | 生成 UUID |
| `formatDate()` | 日期格式化（zh-CN） |
| `formatDateTime()` | 日期时间格式化 |
| `formatAmount()` | 金额格式化（CNY 千分位） |
| `formatFileSize()` | 文件大小格式化 |
| `fileToAttachment()` | 文件转附件对象 |
| `addWatermark()` | 图片水印添加（Canvas） |
| `getCurrentLocation()` | GPS 定位 |
| `getEvidenceStats()` | 证据统计（按状态/类型/金额） |
| `checkEvidenceIntegrity()` | 证据完整性检查 |
| `rankByRisk()` | 风险排序 |

---

## 十一、构建思路分析

### 11.1 核心设计哲学

```
┌────────────────────────────────────────────────────────┐
│                 构建思路金字塔                          │
├────────────────────────────────────────────────────────┤
│                   🏗️ 行业垂直                           │
│              专为土木工程/预算造价设计                   │
│         ┌───────────────────────────────────┐          │
│         │        🤖 AI 增强全流程            │          │
│         │    OCR + 场景 + 签章 + 对话        │          │
│         │  ┌───────────────────────────┐    │          │
│         │  │    💾 本地优先 (Local-First) │    │          │
│         │  │  IndexedDB + PWA + 离线    │    │          │
│         │  │ ┌───────────────────────┐ │    │          │
│         │  │ │ 🔒 零信任 / 零依赖    │ │    │          │
│         │  │ │ 无需服务器/注册/后端  │ │    │          │
│         │  │ └───────────────────────┘ │    │          │
│         │  └───────────────────────────┘    │          │
│         └───────────────────────────────────┘          │
└────────────────────────────────────────────────────────┘
```

### 11.2 为什么选择纯前端架构？

1. **数据主权**：施工单位对证据数据高度敏感，本地存储消除数据泄露顾虑
2. **离线能力**：施工现场网络条件不稳定，PWA 离线可用是刚需
3. **零运维**：个人开发者维护，无需服务器成本
4. **即时可用**：打开浏览器即用，降低用户使用门槛
5. **商业模型**：通过激活码实现授权控制，无需账号系统

### 11.3 为什么选择 Dexie.js + IndexedDB？

1. **浏览器原生**：无需额外安装数据库
2. **容量充足**：IndexedDB 通常可存储数百 MB，满足证据图片存储
3. **Dexie 的优势**：
   - 类 MongoDB 的查询 API
   - `liveQuery()` 实现响应式数据更新
   - 复合索引支持核心查询场景
   - `useLiveQuery()` Hook 直接与 React 集成

### 11.4 AI 集成策略

- **按需调用**：AI 功能（OCR、场景识别等）通过 API Key 配置启用
- **可配置**：用户可在设置页配置自己的 AI API Key
- **容错设计**：未配置 API Key 时，AI 助手显示提示但不影响核心功能
- **上下文感知**：AI 助手根据当前页面（项目详情/证据详情）提供不同上下文

### 11.5 产品迭代路径（从 DB 版本推断）

```
v1: 核心 MVP
    ├── 项目 CRUD
    ├── 证据 CRUD
    └── 基本的状态流转

v2: Schema 稳定化
    └── 兼容性升级

v3: 功能扩展
    └── 施工日志 (dailyLogs)
```

---

## 十二、安全与隐私设计

### 12.1 数据安全

| 层级 | 措施 |
|------|------|
| 传输 | 无数据传输（纯本地） |
| 存储 | IndexedDB（浏览器沙箱隔离） |
| 访问 | 同源策略保护 |
| 图片 | Canvas 水印引擎（时间+位置） |
| 导出 | 用户主动触发 |

### 12.2 授权安全

- 激活码格式校验（正则 + 白名单）
- 授权绑定本机浏览器（localStorage 域隔离）
- 试用时间戳不可篡改（与设备绑定）

---

## 十三、项目亮点总结

### 架构亮点

1. **纯前端 Severless 架构** — 零服务器成本，零运维
2. **IndexedDB 响应式查询** — Dexie `liveQuery` + React Hook 实现实时 UI 更新
3. **PWA 离线可用** — Service Worker + manifest.json
4. **命令面板** — ⌘K 全局搜索，融合命令/项目/证据/日志四域

### 产品亮点

1. **行业垂直深耕** — 10 种证据类型覆盖工程施工全场景
2. **AI 四路并行分析** — OCR + 场景 + 签章 + 质量评估
3. **完整的状态机** — 6 种证据状态 × 智能提醒
4. **图片水印引擎** — Canvas 前端加水印（时间+位置），保护证据真实性
5. **激活码商业模型** — 轻量级授权，适合个人开发者产品

### 工程亮点

1. **Turbopack 构建** — 使用 Next.js 最新 Turbopack，开发体验极佳
2. **Tailwind 4** — 原子化 CSS，暗色主题系统精致
3. **Zustand 状态管理** — 轻量级，按需订阅
4. **React Compiler** — 使用 React 19 的 Memo Cache 优化

---

## 十四、可借鉴的设计模式

### 14.1 Local-First 数据架构

```
Dexie DB → useLiveQuery Hook → React State → UI
    │                                            │
    └──────────── 写入 ──────────────────────────┘
```

适用于：个人工具、笔记应用、离线优先的 SaaS 工具

### 14.2 命令面板模式

融合"页面导航 + 快速操作 + 全局搜索"的统一入口，类似 VS Code 的 Command Palette

### 14.3 状态驱动的 UI 配色

```javascript
// 每个状态映射一套完整的视觉 Token
{bg, border, text, badge, glow, line, hover}
```

### 14.4 渐进式 AI 集成

- AI 功能可选（需配置 API Key）
- 核心功能不依赖 AI
- 用户自主控制 AI 调用

---

## 十五、技术债务与改进空间（推测）

1. **图片存储**：IndexedDB 存储图片有容量上限，大型项目可能需要清理策略
2. **数据备份**：纯本地存储，用户需要手动导出备份
3. **多设备同步**：无云端同步，无法跨设备使用
4. **AI 依赖外部 API**：OCR/场景识别等需要调用外部 AI 服务
5. **微信二维码硬编码**：激活码获取依赖个人微信

---

*本报告基于对 https://evidence.cybertumu.com 前端代码的逆向分析生成。所有技术细节均从公开的 HTML/JavaScript 源码中提取。*
